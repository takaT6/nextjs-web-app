import { SettingsIcon } from "../Icons";

const Settings = () => {
    return (
        <div className="flex m-2  z-50">
            <SettingsIcon />
        </div>
    );
};

export default Settings;